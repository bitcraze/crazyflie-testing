name: crazyflie-lib

on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * 1-5'

jobs:
  linux:
    uses: ./.github/workflows/crazyflie-lib-linux.yml

  macos:
    needs: [linux]
    uses: ./.github/workflows/crazyflie-lib-macos.yml
