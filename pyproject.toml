[project]
name = "crazyflie-testing"
version = "0.1.0"
description = "Tests and infrastructure for testing Bitcraze devices in a physical lab."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest",
    "pytest-timeout",
    "toml",
    "cflib @ git+https://github.com/bitcraze/crazyflie-lib-python.git@rik/rust",
    "pytest-html",
    "pyserial"
]
